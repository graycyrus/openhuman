//! The `bounty_worker` built-in agent: an autonomous, earn-only tiny.place
//! bounty worker. See `prompt.md` for its loop and guardrails, `agent.toml` for
//! its (earn-only) tool allowlist.
pub mod prompt;
